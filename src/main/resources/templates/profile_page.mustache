<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/styles.css">

</head>
<body>
    {{> fragments/error_message}}

    <div class="profile_nav">
        <nav>
            <ul>
                <li><a class="active" href="/Profile">Profile</a></li>
                <li><a href="/Matches">Matches</a></li>
                <li><a href="/Feed">Feed</a></li>
                <li><a href="/Review">Review</a></li>
                <li><a href="/Find-Past-Roommate">Find Past Roommates</a></li>
                <li><a href="/Your-Past-Roommate">Your Past Roommates</a></li>
                <li style="float:right"><a href="/login#logOut">LogOut</a></li>
            </ul>
        </nav>
    </div>

    <div class="profile">
        <div class="profile_info">
            <div class="profile_img">
                <img class="profile_img_link" src="{{user.imagePath}}" alt="Profile Image">
            </div>

            <div class="profile_content">
                <div class="profile_owner">
                    <div class="profile_name">{{user.firstName}} {{user.lastName}}</div>
                    <div class="profile_rate">
                        <h2>{{rate.rating}}/5</h2> 
                    </div>
                </div>

                <div class="profile_desc">{{user.bio}}</div>

                <div class="profile_attrs">
                    <strong>{{user.firstName}}'s Attributes:</strong>
                    <div class="attr-chip-container">
                    {{#user.attributes}}
                        <span class="attr-chip {{category}}">{{name}}</span>
                    {{/user.attributes}}
                    {{^user.attributes}}
                    <span>No attributes listed</span>
                    {{/user.attributes}}
                    </div>
                    {{#isOwnProfile}}
                    <div class="profile_actions">
                        <a href="/profile/attributes" class="action-button">Edit Attributes</a>
                    </div>
                    {{/isOwnProfile}}
                </div>

                <div class="profile_tags">Tags</div>
                <div class="profile_tags">
                    {{#tags}}
                        {{> fragments/profile_tag}}
                    {{/tags}}

                    {{^tags}}
                        <span>No tags listed</span>
                    {{/tags}}
                </div>
            </div>
<<<<<<< HEAD
=======
            <div class="profile_desc">{{user.bio}}</div>
            <div class="profile_attrs">
            <strong>{{user.firstName}}'s Attributes:</strong>
    <div class="attr-chip-container">
        {{#user.attributes}}
            <span class="attr-chip">{{.}}</span>
        {{/user.attributes}}
        {{^user.attributes}}
            <span>No attributes listed</span>
        {{/user.attributes}}
    </div>

    {{#isOwnProfile}}
    <div class="profile_actions">
        <a href="/profile/attributes" class="action-button">Edit Attributes</a>
    </div>
    {{/isOwnProfile}}
</div>

            <div class="profile_tags">Tags</div>
>>>>>>> 79cfbe088a68deff5a8d4215c9f139f85dfafaa8
        </div>
    </div>

    <div class="profile_divider">
        <h1>Reviews for {{user.firstName}}</h1>
    </div>

    <div class="profile_reviews"> 
        {{#reviews}}
            {{> fragments/profile_review}}
        {{/reviews}}

        {{^reviews}}
            <p>No reviews yet!</p>
        {{/reviews}}
    </div>
</body>
</html>
